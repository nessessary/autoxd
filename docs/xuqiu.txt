v0.5.0
- 分别构建cnn的形态识别和硬编码的形态识别， 针对一个短期的boll图
- 实现一个单元测试， 输入一幅boll图， 用上述两种方法判断其买卖percent

v0.4.2
- gym实现
- 0.4.2为最终支持py2的版本
	
v0.4
- numba测试, 带有pandas使用不起来
- 优化回测的速度
- 用DQN跑随机回测, 每一个回测作为一个状态

v0.3
- 用py3重新实现读取ths的页面数据, 只针对一个代码的读取， 取消原有的整体爬取方式
	1) 读取分红表, 支持前复权
	2) 读取股本变更表, 支持成交量转换手率

- 获取日线后强制转换，套用前复权和转换为换手率
	1)先支持日线
	2) 后期支持5分钟线, 废弃分时支持

- 代码名称对照

- 数据源默认只支持LiveData， 其它使用自定义方式
	1) 自定义使用一个Tushare的例子

- 去除客户端使用的前复权计算

- 修正report图表的date显示
	1) 左边的显示斜角度日期
	2) 右边添加间隔

- py3 ui里的legand不会躲避